# BudgetFlow backend (auditflow frontend)
# Copy to .env.local

# Use proxy so same-origin requests avoid CORS (recommended in dev)
NEXT_PUBLIC_USE_PROXY=true

# Or set backend URL directly (e.g. if backend runs on another port)
# NEXT_PUBLIC_API_URL=http://localhost:8000
